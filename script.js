const menuBtn=document.querySelector('.menu-btn'),navlinks=document.querySelector('.navlinks');if(menuBtn&&navlinks){menuBtn.onclick=()=>navlinks.classList.toggle('open');navlinks.querySelectorAll('a').forEach(a=>a.onclick=()=>navlinks.classList.remove('open'))}const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(x=>obs.observe(x));const fill=document.querySelector('.progress-fill'),label=document.querySelector('.route-label'),stops=[...document.querySelectorAll('.stop[data-stop]')];function prog(){if(!fill||!stops.length)return;let n=0;stops.forEach((s,i)=>{if(s.getBoundingClientRect().top<innerHeight*.55)n=i+1});fill.style.width=(n/stops.length*100)+'%';if(label)label.textContent=n?`Parada ${n} de ${stops.length}`:'Início da rota'}addEventListener('scroll',prog,{passive:true});prog();
